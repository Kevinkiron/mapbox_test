import 'package:flutter/material.dart';

class AppColors {
  static const whitecolor = Color(0xffffffff);
  static const black = Color(0xff000000);
  static const primaryblue = Color(0xFF256af5);
  static const transparent = Colors.transparent;
}
